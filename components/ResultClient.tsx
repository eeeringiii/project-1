'use client';import Link from'next/link';import {getType}from'@/data/types';import {useDiagnosisStore}from'@/stores/diagnosis';import {Radar}from'@/components/Radar';import {Share}from'@/components/Share';import {OshicoaType}from'@/types';export default function ResultClient({type}:{type:OshicoaType}){const {result,reset}=useDiagnosisStore();const current=result?.typeCode===type?.code?result:undefined;const download=()=>{if(!type)return;const svg=`<svg xmlns="http://www.w3.org/2000/svg" width="1080" height="1080"><rect width="100%" height="100%" fill="#090914"/><text x="80" y="130" fill="#f4f2ff" font-size="44" font-family="Arial">OSHICOA 16</text><text x="80" y="420" fill="#e8c7ff" font-size="180" font-family="Arial" font-weight="bold">${type.code}</text><text x="80" y="530" fill="white" font-size="76" font-family="Arial">${type.name}</text><text x="80" y="620" fill="#d0cae1" font-size="40" font-family="Arial">${type.catchphrase}</text><text x="80" y="960" fill="#f472b6" font-size="32" font-family="Arial">#OSHICOA16 #ヲタク生態診断</text></svg>`;const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([svg],{type:'image/svg+xml'}));a.download=`oshicoa16-${type.code}.svg`;a.click();URL.revokeObjectURL(a.href)};const tags=current?.tags||[];return <main><section className="resultHero"><div className="shell"><p className="eyebrow">YOUR OTAKU ECOLOGY</p><p>{current?.oshiProfile?.name?`${current.oshiProfile.name}を推しているときのあなたは`:'あなたのヲタク生態は'}</p><div className="code">{type.code}</div><h1>{type.name}</h1><p className="catch">{type.catchphrase}</p>{tags.length>0&&<p>{tags.map(t=><span className="tag" key={t.id}>#{t.name}</span>)}</p>}<Share type={type} tags={tags} name={current?.oshiProfile?.name}/></div></section><div className="shell resultGrid"><article><p className="eyebrow">ABOUT YOU</p><h2>あなたの推し方</h2>{type.description.map(p=><p key={p}>{p}</p>)}<h2>ヲタクあるある</h2><ul>{type.habits.map(x=><li key={x}>{x}</li>)}</ul><h2>強み</h2><ul>{type.strengths.map(x=><li key={x}>{x}</li>)}</ul><h2>愛おしい業</h2><ul>{type.shadowTraits.map(x=><li key={x}>{x}</li>)}</ul></article><aside><p className="eyebrow">ABILITY PROFILE</p><Radar scores={current?.chartScores||type.chartBaseValues}/><p className="tag">推定 {type.estimatedRatio}%</p>{current?<section className="item"><b>現在の関係フェーズ：{current.phase.name}</b><p>{current.phase.description}<br/>{current.phase.advice}</p></section>:<p className="lead">回答に基づく業タグ・関係フェーズは、診断後に表示されます。</p>}<section className="item"><b>相性がよいタイプ</b><p><Link href={`/types/${type.compatibility.bestTypeCode}`}>{getType(type.compatibility.bestTypeCode)?.name}</Link> — {type.compatibility.description}</p></section><button className="button ghost" onClick={download}>結果画像を保存（正方形）</button><p><Link className="button" href={`/fortune?type=${type.code}`}>このタイプで今日の運勢を占う　→</Link></p><p><Link className="button ghost" href="/diagnosis" onClick={()=>reset(true)}>同じ推しで再診断</Link></p></aside></div></main>}
