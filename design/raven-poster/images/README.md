# 写真リンク置き場

`raven-poster.svg` はこのフォルダの以下2ファイルをリンク参照しています。

| ファイル名 | 内容 |
| --- | --- |
| `raven-bg.jpg` | 背景写真（ダイナー内装）。高解像度JPG |
| `raven-members.png` | 人物5人の切り抜き。背景透過PNG |

合成済みの1枚しか無い場合は `raven-bg.jpg` にそれを入れ、
SVGの `人物` サブレイヤーを非表示にしてください。
