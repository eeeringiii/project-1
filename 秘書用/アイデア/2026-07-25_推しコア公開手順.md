# 推しコア（OSHICOA 16）公開状況メモ

## 結論：**すでに公開済み**

- **公開URL（本番）: https://oshicoa16.vercel.app**
- ヲタク生態診断（推しコア本体）が全ページ稼働・一般公開中（認証ウォールなし・2026-07-25 秘書確認）
- HP（`https://project-1-zeta-sooty.vercel.app` ＝ `taisei-site`）とは**別プロジェクト**として独立稼働

> 最初「未公開」と判断したのは、このURLがリポジトリ／秘書メモのどこにも記録されていなかったため。
> 実体はVercel側に8日前（2026-07-17）から存在していた。以後はこのメモをURLの正本とする。

## Vercelプロジェクト情報

| 項目 | 値 |
|---|---|
| プロジェクト名 | `oshicoa16`（チーム: eringi） |
| 本番URL | https://oshicoa16.vercel.app |
| リポジトリ | `eeeringiii/project-1`（GitHub連携・自動デプロイ） |
| Root Directory | `.`（リポジトリ直下＝推しコア本体） |
| Framework | Next.js（Build: `npm run build`） |

## 環境変数

- `NEXT_PUBLIC_SITE_URL = https://oshicoa16.vercel.app`（2026-07-25 設定）
  - 共有リンク・sitemap・動的OG画像の基準URL。独自ドメイン接続時はこの値を差し替えて再デプロイするだけ。
- `ANTHROPIC_API_KEY`：**未設定でOK**。診断はブラウザ内で完結。`/api/analyze-food` `/api/feedback` は食事分析の実験用エンドポイントで診断とは無関係（使う場合のみ後から追加）。

## 更新のしかた

GitHub連携済みのため、**秘書がリポジトリを更新（本番ブランチにマージ）すれば自動で本番に反映**される。手動で今すぐ反映したいときは Vercel の Deployments → Redeploy。

## 今後（任意）

- 独自ドメイン接続（`NEXT_PUBLIC_SITE_URL` を新ドメインに差し替え → 再デプロイ）
- ファンへ広く公開する前に、README「仮データ」節（能力値・推定割合・相性文）の実測差し替えを検討
