import {notFound}from'next/navigation';import {getType,oshicoaTypes}from'@/data/types';import {Radar}from'@/components/Radar';import Link from'next/link';export function generateStaticParams(){return oshicoaTypes.map(t=>({typeCode:t.code}))}export default async function TypePage({params}:{params:Promise<{typeCode:string}>}){const {typeCode}=await params;const t=getType(typeCode);if(!t)notFound();return <main className="shell"><section className="resultHero"><p className="eyebrow">ECOLOGY FILE / {t.code}</p><h1>{t.name}</h1><p className="catch">{t.catchphrase}</p><Link className="button" href="/diagnosis">このタイプか診断する</Link></section><div className="resultGrid"><article>{t.description.map(x=><p key={x}>{x}</p>)}<h2>こんな推し方</h2><ul>{t.habits.map(x=><li key={x}>{x}</li>)}</ul></article><Radar scores={t.chartBaseValues}/></div></main>}
