'use client';import {useState}from'react';import Link from'next/link';import {oshicoaTypes}from'@/data/types';const keys=['R','C','E','P','G','M','T','S'];export default function Types(){const [filters,setFilters]=useState<string[]>([]);const shown=oshicoaTypes.filter(t=>filters.every(f=>t.code.includes(f)));return <main className="shell section"><p className="eyebrow">ECOLOGY INDEX</p><h1>16のヲタク生態</h1><p className="lead">コードは入口。主役は、あなたが何を守り、何に熱くなるかです。</p><p>{keys.map(k=><button className={`filter ${filters.includes(k)?'active':''}`} key={k} onClick={()=>setFilters(x=>x.includes(k)?x.filter(a=>a!==k):[...x,k])}>{k}</button>)}</p><div className="grid">{shown.map(t=><Link className="type" href={`/types/${t.code}`} key={t.code}><span className="eyebrow">{t.code}</span><h3>{t.name}</h3><p>{t.catchphrase}</p><small>詳細を見る →</small></Link>)}</div></main>}
