import {ImageResponse}from'next/og';import {getType}from'@/data/types';export const runtime='edge';export async function GET(request:Request){const code=new URL(request.url).searchParams.get('type')||'CEMS';const type=getType(code)||getType('CEMS')!;return new ImageResponse(<div style={{height:'100%',width:'100%',display:'flex',flexDirection:'column',justifyContent:'center',background:'#090914',color:'#fff',padding:72,fontFamily:'sans-serif'}}><span style={{fontSize:32,color:'#f0abfc'}}>OSHICOA 16</span><strong style={{fontSize:120,marginTop:45,color:'#ddd6fe'}}>{type.code}</strong><span style={{fontSize:68}}>{type.name}</span><span style={{fontSize:34,color:'#d4d0dd',marginTop:25}}>{type.catchphrase}</span></div>,{width:1200,height:630})}
