import type {MetadataRoute}from'next';import {oshicoaTypes}from'@/data/types';export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||'https://oshicoa16.example.com';return ['','/diagnosis','/types','/fortune','/bingo','/about','/privacy','/terms','/compatibility'].map(url=>({url:base+url,lastModified:new Date()})).concat(oshicoaTypes.map(t=>({url:`${base}/types/${t.code}`,lastModified:new Date()})))}
